<% if not flash.empty? then %>
<div class="flash">
  <% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id=>"flash_#{name}" %>
  <% end %>            <!--                          <%# content_tag :h1, yield(:title) if show_title? %>                     -->
</div>
<% end %>
